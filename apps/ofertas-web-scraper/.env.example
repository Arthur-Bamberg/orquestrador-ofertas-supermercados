# Copie para `.env` se for `go run` neste diretório.
# No Compose, a raiz `.env` + hostname `postgres` prevalece.

DATABASE_URL=postgres://ofertas:ofertas@localhost:5432/ofertas?sslmode=disable
SEED_PATH=./seed/mercados.json
TZ=America/Sao_Paulo
